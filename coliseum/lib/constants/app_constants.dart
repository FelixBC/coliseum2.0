import 'package:flutter/material.dart';

// Spacing Constants
const EdgeInsets kPaddingSmall = EdgeInsets.all(8.0);
const EdgeInsets kPaddingMedium = EdgeInsets.all(16.0);
const EdgeInsets kPaddingLarge = EdgeInsets.all(24.0);

// Border Radius Constants
const BorderRadius kBorderRadiusSmall = BorderRadius.all(Radius.circular(8.0));
const BorderRadius kBorderRadiusMedium = BorderRadius.all(Radius.circular(12.0));
const BorderRadius kBorderRadiusLarge = BorderRadius.all(Radius.circular(16.0)); 